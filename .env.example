SECRET_KEY=change-me-to-a-long-random-string
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME
MAPBOX_TOKEN=
# Optional: change where uploads are stored (ensure path is writable)
# UPLOAD_BASE=/path/to/uploads
